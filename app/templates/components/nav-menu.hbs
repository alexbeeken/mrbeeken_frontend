{{#if show}}
  <nav class='main-nav'>
    <div class='dismiss-menu'>
      <button {{action 'dismissMenu'}}>{{fa-icon 'times'}}</button>
    </div>
    <ul>
      <!-- <li>
        {{component 'nav-link' text='blog' path='posts' icon='newspaper-o'}}
      </li> -->
      <!-- <li>
        {{component 'nav-link' text='projects' path='projects' icon='video-camera'}}
      </li> -->
      <li>
        {{component 'nav-link' text='about' path='about.experience' icon='info-circle'}}
      </li>
      {{#if superuser}}
        <li>
          {{component 'nav-link' text='admin' path='admin' icon='cog'}}
        </li>
      {{/if}}
    </ul>
  </nav>
{{else}}
<div class='show-menu'>
  <button {{action 'showMenu'}}>{{fa-icon 'bars'}}</button>
</div>
{{/if}}
